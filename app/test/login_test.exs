defmodule AuthenticationTest do
  # use ExUnit.Case
  #
  # @test_data %{email: "hello@example.com", password: "password"}
  #
  # test "when user not found should report not found" do
  #   assert Authentication.login(nil, @test_data) == "can't find email"
  # end
  #
  # test "passwords match" do
  #   assert Authentication.login(%User{password: "password1"}, @test_data) == "email and password do not match"
  # end
  #
  # test "not confirmed" do
  #   assert Authentication.login(%User{password: "password"}, @test_data) == "not confirmed"
  # end
  #
  # test "success" do
  #   assert Authentication.login(%User{password: "password", confirmed_at: "now"}, @test_data) == "success"
  # end
end
