defmodule FizzBuzzTest do
  use ExUnit.Case
  # doctest FizzBuzz
  #
  # test "FizzBuzz returned if the 1st two arguments are zero" do
  #   assert FizzBuzz.run(0, 0, 3) == "FizzBuzz"
  # end
  #
  # test "Fizz returned if the 1st argument is zero and the 2nd argument isn't" do
  #   assert FizzBuzz.run(0, 1, 3) == "Fizz"
  # end
  #
  # test "Buzz returned if the 1st argument isn't zero and the 2nd argument is" do
  #   assert FizzBuzz.run(1, 0, 3) == "Buzz"
  # end
  #
  # test "the 3rd returned if both of the 1st two arguments aren't zero" do
  #   assert FizzBuzz.run(1, 2, 3) == 3
  # end
end
